python:
  coverage:
    threshold: 85
    paths:
      - src
  complexity:
    max: 8
  dry:
    max_findings: 0
    paths:
      - src
    copied_blocks:
      enabled: true
      min_tokens: 100
  mutation:
    targets:
      - src/hf_auth_helper/scopes.py
